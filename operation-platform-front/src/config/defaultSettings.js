export default {
  primaryColor: '#1890FF',
  navTheme: 'dark',
  // 默认顶部菜单格式
  layout: 'topmenu',
  // 内容宽度 固定
  contentWidth: 'Fixed',
  // 固定头部
  fixedHeader: false,
  // 是否固定
  fixSidebar: false,
  // 是否隐藏
  autoHideHeader: false,
  // 存储属性
  storageOptions: {
    namespace: 'mlnlco__',
    // 名称 ls
    name: 'ls',
    // 存储类型 local? cookie ?
    storage: 'local'
  }
}
